project:
  repo: https://github.com/carlyou/vllm.git
  model: nvidia/DeepSeek-V3.2-NVFP4

build:
  use_precompiled: true
  max_jobs: 0.8
  cuda_arch: "10.0"
  torch_index: https://download.pytorch.org/whl/cu130

server:
  tp: 4

eval:
  script: tests/evals/gsm8k/gsm8k_eval.py
  args: "--chat --num-shots 5 --num-questions 1319 --max-tokens 256 --temperature 0.0 --seed 42"

# 4xB200: DeepSeek-V3.2 NVFP4, TP4. gsm8k eval with fusion.

branches:
  test/mla-group-fp8-fusion:
    runs:
      - label: gsm8k_fuse_on_eager
        server:
          enforce_eager: true
          compilation_config:
            pass_config:
              fuse_attn_quant: true
