project:
  repo: https://github.com/carlyou/vllm.git
  model: RedHatAI/DeepSeek-Coder-V2-Lite-Instruct-FP8

build:
  use_precompiled: true
  max_jobs: 0.8
  cuda_arch: "9.0"

server:
  enforce_eager: true

bench:
  request_rate: 50

# H100: SM 90, eager mode (no torch.compile, no cudagraphs).
# Cross-check to isolate compile/cudagraph effects from branch differences.

branches:
  main:
    runs:
      - label: main_warmup
      - label: main_eager

  feat--mla-static-quant-fusion:
    runs:
      - label: feature_fuse_off_eager

      - label: feature_fuse_on_eager
        server:
          compilation_config:
            pass_config:
              fuse_attn_quant: true
