project:
  repo: https://github.com/carlyou/vllm.git
  model: deepseek-ai/DeepSeek-V3

build:
  cuda_arch: "10.0"
  torch_index: https://download.pytorch.org/whl/cu130

server:
  tp: 4
  max_model_len: 4096
  gpu_memory_utilization: 0.95
  log_level: debug
  wait_timeout: 1800

bench:
  num_prompts: 1000
  input_len: 128
  output_len: 128
  request_rate: inf
  warmup_prompts: 3
  num_warmups: 50

# 4xB200: DSV3 FP8 benchmark — main baseline vs feature fusion on.
# Both use torch.compile with cudagraph NONE for fair comparison.

branches:
  main:
    use_precompiled: true
    runs:
      - label: main_warmup
        server:
          compilation_config:
            cudagraph_mode: NONE
            custom_ops: ["+quant_fp8"]
        bench:
          num_prompts: 10
          num_warmups: 5

      - label: main_baseline
        server:
          compilation_config:
            cudagraph_mode: NONE
            custom_ops: ["+quant_fp8"]

  feat--mla-quant-fusion-group-fp8:
    runs:
      - label: feat_fuse_on
        server:
          compilation_config:
            cudagraph_mode: NONE
            custom_ops: ["+quant_fp8"]
            pass_config:
              fuse_attn_quant: true
