project:
  repo: https://github.com/carlyou/vllm.git
  model: RedHatAI/DeepSeek-Coder-V2-Lite-Instruct-FP8

build:
  use_precompiled: true
  max_jobs: 0.8

server:
  enforce_eager: true

eval:
  script: tests/evals/gsm8k/gsm8k_eval.py
  args: "--num-shots 5 --num-questions 10 --max-tokens 256 --temperature 0.0 --seed 42"

# DGX Spark: tiny eval run to smoke-test the pipeline.

branches:
  feat--mla-static-quant-fusion:
    runs:
      - label: feature_fuse_off_eager
        server:
          compilation_config:
            pass_config:
              fuse_attn_quant: false

      - label: feature_fuse_on_eager
        server:
          compilation_config:
            pass_config:
              fuse_attn_quant: true
